[Unit]
Description=KDE Connect Daemon (Headless & Desktop)
After=default.target

[Service]
Type=simple
Environment=QT_QPA_PLATFORM=offscreen
ExecStart=/usr/bin/kdeconnectd
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
